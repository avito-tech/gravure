 {
"presets": {
    "preset1": {
        "name": "preset1",
        "tasks": [
            {
            "name": "task1",
            "actions": [
                    [ "resize", "500", "250" ],
                    [ "save", "./{{node_id}}_{{image_id}}.{{ext}}" ],
                    [ "upload", "http://127.0.0.1:4444/upload/test" ]
                ],
            "url_template": "http://{{node_id}}/protools/{{task_name}}/{{image_id}}"
            }
            ]
        }
    }
}
